# Example environment variables for the NestJS API
# Copy this file to `.env` and update values for your environment.
# NEVER commit real secrets to source control.

############################
# App
############################
NODE_ENV=development
PORT=5000

############################
# Database (MySQL)
############################
# Hostname or IP of the MySQL server
DB_HOST=localhost
# MySQL port (default 3306)
DB_PORT=3306
# MySQL credentials
DB_USER=root
DB_PASS=
DB_NAME=bfp_sorsogon_attendance

# Optional TypeORM toggles (used by some setups/tooling)
TYPEORM_SYNCHRONIZE=false
TYPEORM_LOGGING=false

############################
# Auth / JWT
############################
# Secret used to sign JWTs. Replace with a secure random value in production.
JWT_SECRET=dev-jwt-secret
# Token lifetime (e.g. '1h', '7d')
JWT_EXPIRES_IN=1h

############################
# Face recognition microservice
############################
# URL for the separate Python face-recognition service
# Example: http://localhost:5001
FACE_SERVICE_URL=http://localhost:5001

############################
# Frontend / CORS
############################
# URL of the Next.js frontend (used for CORS and cookie domains)
FRONTEND_URL=http://localhost:3000

############################
# Optional / infra
############################
# Enable Swagger UI (true/false)
SWAGGER_ENABLED=true

# Redis/Cache URL (optional)
# REDIS_URL=redis://localhost:6379

# Sentry DSN (optional)
# SENTRY_DSN=

# Notes:
# - After copying to `.env`, update values (especially secrets) before running in production.
# - Keep `.env` out of version control.
